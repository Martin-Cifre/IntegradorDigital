// login.js

/* document.addEventListener("DOMContentLoaded", function () {
    const enlacePerfil = document.getElementById("perfilButton");
    const usuarioLogueado = enlacePerfil.getAttribute("data-usuario-logueado") === "true";
  
    if (usuarioLogueado) {
      enlacePerfil.href = "/perfil";  // Reemplaza con la URL de perfil real
    } else {
      enlacePerfil.href = "/login";  // Reemplaza con la URL de inicio de sesión real
    }
  });
   */